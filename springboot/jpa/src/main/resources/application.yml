spring:
  application:
    name: jpa

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/demodb
    username: sa
    password: 1234

  h2:
    console:
      enabled: true
      path: /h2-console # Web url

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true